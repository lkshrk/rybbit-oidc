---
title: Dashboard Embed
description: Embed your public analytics dashboard on another site
---

import { Callout } from 'fumadocs-ui/components/callout';

The dashboard embed lets you place the main Rybbit analytics dashboard on another site with a self-contained iframe. It uses the normal public dashboard route with `embed=true`.

## Requirements

Dashboard embeds require **Public Analytics** to be enabled for the site. Unlike [Widget Embeds](/docs/embeds/widget), this makes the site's read-only analytics dashboard accessible to anyone with the URL.

<Callout type="warn">
  Only enable Public Analytics if you are comfortable exposing the site's dashboard data publicly.
</Callout>

## Setup

1. Open your site in the Rybbit dashboard and click the settings icon.
2. Go to the **Dashboard Embed** tab.
3. Toggle **Public Analytics** on.
4. Choose the embed theme: light, dark, or system.
5. Choose whether to hide the dashboard sidebar.
6. Copy the generated `<iframe>` snippet and paste it anywhere on your site.

## Appearance

- **Theme**: `light`, `dark`, or `system`. `system` follows the viewer's operating system color-scheme preference.
- **Hide sidebar**: removes the dashboard sidebar and keeps viewers on the main dashboard page.
- **Footer**: the Rybbit footer is hidden automatically in embed mode.

## Example

```html
<iframe
  src="https://app.rybbit.io/YOUR_SITE_ID/main?embed=true&theme=system&hideSidebar=true"
  style="border: 0; width: 100%; height: 720px;"
  loading="lazy"
  title="Analytics dashboard"
></iframe>
```

If you self-host Rybbit, replace `app.rybbit.io` with your own domain.

## URL Parameters

| Parameter     | Values                   | Description                                                                 |
| ------------- | ------------------------ | --------------------------------------------------------------------------- |
| `embed`       | `true`                   | Enables public embed mode                                                   |
| `theme`       | `light`, `dark`, `system` | Sets the embedded dashboard color scheme. Defaults to `system`              |
| `hideSidebar` | `true`, `false`          | Hides the dashboard sidebar and keeps visitors on the main dashboard page   |

When `hideSidebar=true`, only the main dashboard page is shown in the iframe. Leave it off if you want viewers to use the dashboard sidebar to navigate other public analytics pages.
